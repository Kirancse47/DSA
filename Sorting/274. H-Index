    int hIndex(vector<int>& cit) {
        int n=cit.size();
        sort(cit.begin(),cit.end());
        for(int i=0;i<n;i++){
            int h=n-i;
            if(cit[i]>=h) return h;
        }
        return 0;
    }
