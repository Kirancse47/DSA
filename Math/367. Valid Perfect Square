    bool isPerfectSquare(int num) {
        if(num==1) return true;
        int l=1,h=num/2;
        while(l<=h){
            long long m=l+(h-l)/2;
            if(m*m==num) return true;
            else if(m*m<num) l=m+1;
            else h=m-1;
        }
        return false;
    }
