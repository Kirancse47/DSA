    vector<bool> prefixesDivBy5(vector<int>& nums) {
        int n=nums.size();
        int num=0;
        vector<bool> ans(n,false);
        int mod=1e9+7;
        for(int i=0;i<n;i++){
            num=((num<<1) + nums[i])%5;
            ans[i]=(num==0);
        }
        return ans;
    }
